package org.example.Exercices.ImageViewerProxy;

//BanqueAccess Dans l'exercice : L'interface Image
// (Puisqu'on voit dans le main que le proxy est stocké dans une variable de type Image).
// Elle devra définir la méthode showImage(User user).
public interface Image {
    void showImage(User user);
}
